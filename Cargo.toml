[package]
name = "pg_petgraph"
version = "0.1.0"
edition = "2021"
description = "PostgreSQL graph algorithm extension — PageRank, SCC, Betweenness Centrality"
license = "MIT"
repository = "https://github.com/alitrack/pg_petgraph"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "pgrx_embed_pg_petgraph"
path = "./src/bin/pgrx_embed.rs"

[features]
default = ["pg17"]
pg17 = ["pgrx/pg17", "pgrx-tests/pg17"]
pg16 = ["pgrx/pg16", "pgrx-tests/pg16"]
pg15 = ["pgrx/pg15", "pgrx-tests/pg15"]
pg_test = []

[dependencies]
pgrx = { version = "0.13", default-features = false }
petgraph = "0.7"

[dev-dependencies]
pgrx-tests = { version = "0.13", default-features = false }

[profile.release]
panic = "unwind"
opt-level = 3
lto = "fat"
codegen-units = 1

[profile.ci]
inherits = "release"
lto = "thin"
codegen-units = 4
opt-level = 2
